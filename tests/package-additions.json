{
  "_comment": "Merge these into package.json. Do NOT copy this file wholesale — it is a spec, not a package manifest.",
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "playwright": "^1.47.0",
    "vitest": "^2.1.0",
    "tsx": "^4.19.0"
  },
  "scripts": {
    "test": "playwright test --config=tests/playwright.config.ts",
    "test:unit": "vitest run --config tests/vitest.config.ts",
    "test:unit:watch": "vitest --config tests/vitest.config.ts",
    "pretest": "npm run build:ts"
  }
}
